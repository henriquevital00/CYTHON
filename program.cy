number a = 6;
number b = 7;
bool x;

if a == b {
    a = 5;
    b = 6;

    if c == d & d == c{
        if (c == d & d == c) | c == d{
            a = 5;

            while b == c & c == d{

            };
        };
    };
};