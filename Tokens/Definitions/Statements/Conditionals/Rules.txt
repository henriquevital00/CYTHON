#              TOKENS PARA STATEMENTS CONDICIONAIS
# ==========================================================
#   TOKEN           REGEX              MEANING
# ==========================================================
#    IF              if         IF conditional token
#   ELIF            elif        ELSE IF conditional token
#   ELSE            else        ELSE conditional token
#
#  TOKEN TYPE: KEYWORD
#  REGEX if|else|elif
